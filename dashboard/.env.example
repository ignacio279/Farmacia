# API del backend (NestJS). Mismo origen que el backend en desarrollo.
NEXT_PUBLIC_API_URL=http://localhost:3000

# Misma clave que SEND_API_KEY del backend (para autorizar requests al panel).
NEXT_PUBLIC_SEND_API_KEY=
